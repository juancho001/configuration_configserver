# Spring Boot application configuration for ms-report service
server:
  port: 0
#  servlet:
#    context-path: /ms-report
    
# Configuration for Eureka Client
eureka:
  instance:
    instance-id: ${spring.application.name}:${random.value}
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
